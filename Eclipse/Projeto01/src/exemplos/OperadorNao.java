package exemplos;

public class OperadorNao{
		  public static void main(String[] args) {
		    int valorInteiro = 5;
		    System.out.println(!(valorInteiro > 3 && valorInteiro < 10)); // returns false because ! (not) is used to reverse the result
		  }
		}

